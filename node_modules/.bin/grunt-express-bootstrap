#!/bin/sh
basedir=`dirname "$0"`

case `uname` in
    *CYGWIN*) basedir=`cygpath -w "$basedir"`;;
esac

if [ -x "$basedir/node" ]; then
  "$basedir/node"  "$basedir/../grunt-express-bootstrap/bin/grunt-express-bootstrap" "$@"
  ret=$?
else 
  node  "$basedir/../grunt-express-bootstrap/bin/grunt-express-bootstrap" "$@"
  ret=$?
fi
exit $ret
